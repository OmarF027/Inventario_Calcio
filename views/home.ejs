<%- include('partials/header', {title: 'Home - Inventario Calcio'}) %>

<h2>Benvenuto nell'Inventario!</h2>

<% if (categorie && categorie.length > 0) { %>
    <h3>Categorie:</h3>
    <ul>
        <% categorie.forEach(categoria => { %>
            <li><strong><%= categoria.nome %></strong> - <%= categoria.descrizione %></li>
        <% }); %>
    </ul>
<% } else { %>
    <p>Nessuna categoria trovata.</p>
<% } %>

<% if (articoli && articoli.length > 0) { %>
    <h3>Articoli:</h3>
    <ul>
        <% articoli.forEach(articolo => { %>
            <li><strong><%= articolo.nome %></strong> - Quantità: <%= articolo.quantita %></li>
        <% }); %>
    </ul>
<% } else { %>
    <p>Nessun articolo trovato.</p>
<% } %>

<div style="margin-top: 30px; display: flex; gap: 15px;">
    <a href="/categorie" style="display: inline-block; padding: 12px 25px; background: #0055a4; color: white; text-decoration: none; border-radius: 3px;">
        📋 Vedi Tutte le Categorie →
    </a>
    
    <a href="/articoli/nuovo" style="display: inline-block; padding: 12px 25px; background: #28a745; color: white; text-decoration: none; border-radius: 3px;">
        📦 Aggiungi Nuovo Articolo
    </a>
</div>

<div style="margin-top: 15px;">
    <a href="/articoli" style="display: inline-block; padding: 10px 15px; background: #6c757d; color: white; text-decoration: none; border-radius: 3px;">
        📊 Vedi Tutti gli Articoli
    </a>
</div>

<%- include('partials/footer') %>